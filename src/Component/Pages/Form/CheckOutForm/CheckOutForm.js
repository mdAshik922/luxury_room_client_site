import React from 'react';
import useAuth from '../../../Hooks/useAuth';

const CheckOutForm = () => {

    const { user } = useAuth();
    return (
        <div>
            
        </div>
    );
};

export default CheckOutForm;